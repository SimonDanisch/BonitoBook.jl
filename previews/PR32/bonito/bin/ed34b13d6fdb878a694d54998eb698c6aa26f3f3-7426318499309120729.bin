’Ç,j“Ù$78972608-f4fd-4ee2-abf3-ac6ec1702c8c€£subÇék’Ç-j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c€¤root‚§payloadÇ¡f“€Ù]    window.BONITO_EXPORT_MODE = true;
    document.body.classList.add('bonito-export-mode');
Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:96¨msg_type¡2È©k’Ç-j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c€¤root‚§payloadÈ`f“€Ú(() => {
    document.querySelector('[data-jscall-id="28"]')['onclick'] = () => {
    const a = document.createElement('a');
a.href = '../../bonito/md/post16588186637605283268.md';
a.download = 'post.md';
document.body.appendChild(a);
a.click();
document.body.removeChild(a);

}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:78¨msg_type¡2Çïk’Ç-j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c€¤root‚§payloadÇ§f“€Ùc(() => {
    document.querySelector('[data-jscall-id="30"]')['onclick'] = ()=> window.print();
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:85¨msg_type¡2È…k’Ç=j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14258×e’¥14258Â¤root‚§payloadÈ,f“´11420052658563274061Çh¥14258ÚÉ(() => {
    document.querySelector('[data-jscall-id="36"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('11420052658563274061').value;
    __lookup_interpolated('11420052658563274061').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2È‚k’Ç=j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14259×e’¥14259Â¤root‚§payloadÈ)f“³3859950897308449529Çh¥14259ÚÇ(() => {
    document.querySelector('[data-jscall-id="38"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('3859950897308449529').value;
    __lookup_interpolated('3859950897308449529').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2È‚k’Ç=j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14257×e’¥14257Ã¤root‚§payloadÈ)f“³7092812914102450203Çh¥14257ÚÇ(() => {
    document.querySelector('[data-jscall-id="40"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('7092812914102450203').value;
    __lookup_interpolated('7092812914102450203').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2ÈHk’Ç=j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14260×e’¥14260Â¤root‚§payloadÇğf“²737170362037220324Çh¥14260Ù(() => {
    document.querySelector('[data-jscall-id="42"]')['onclick'] = event=> __lookup_interpolated('737170362037220324').notify(true)
})()Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:451¨msg_type¡2È=k’È=j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016cŠ¥14225Çe’¥14225§default¥14236×e’¥14236Â¥14235×e’¥14235Ã¥14241×e’¥14241Â¥14226×e’¥14226Â¥14228×e’¥14228€¥14261×e’¥14261Â¥14227×e’¥14227€¥14232Ène’¥14232Úd# Plugin System, Language Extensions & Simplified Styling

We're excited to announce a major update to BonitoBook that transforms it from a single-purpose notebook into an extensible platform. This release introduces a powerful plugin system, extensible language evaluators, and a completely redesigned styling architecture.

## 1. Plugin System: Transform Your Notebooks

The biggest new feature is a flexible plugin system that lets you completely customize how notebooks are rendered and behave. Plugins are modules that define custom book types and rendering logic.

### Creating a Plugin

Plugins live in `.name-bbook/book.jl` or a shared `.bbook/book.jl`:

```julia
module MyPlugin
using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end
# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module
```

From the [plugin guide](/howto/bonitobook-plugin):

### Built-in Plugin Examples

We've included three example plugins showcasing different use cases:

**Slideshow Plugin** - [Turn notebooks into presentations](../../examples/slideshow_example)

**Draggable Plugin** - [Interactive drag-and-drop layouts](../../examples/draggable_example)

**Book Plugin** - [Custom book formats with enhanced styling and table of content](../../examples/book-example)

## 2. Extensible Language Evaluators

BonitoBook now supports an extensible evaluator system for any language. We've moved language-specific code into extensions and made it easy to add new languages. Python support now needs using PythonCall.jl and CondaPkg:

```julia
# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution
```

Variables are automatically transferred between Python and Julia scopes, and the Python environment persists across cells.

### Creating Custom Language Evaluators

Adding a new language to BonitoBook is straightforward. From the [language evaluator guide](/howto/language-evaluator):

```julia
# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end
```

### Dependency Reduction

As part of this refactoring, we've moved WGLMakie to an extension, significantly reducing BonitoBook's dependency footprint and load times. Makie is still fully supported but only loaded when needed.

## 3. Simplified Styling System

The styling system has been completely redesigned around a single `generate_style()` function with comprehensive CSS variables.

### Before and After

**Before**: Styles scattered across `styles/style.jl` with hardcoded values everywhere

**After**: Single `style.jl` file with 70+ optional parameters:

```julia
style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object
```

### Lazy File Loading

The new folder structure uses lazy initialization:

```
.name-bbook/
â”œâ”€â”€ book.jl      # Plugin definition (optional)
â”œâ”€â”€ style.jl     # Styling (lazy-loaded from template)
â”œâ”€â”€ include.jl   # Optional include file, with code run on start
â”œâ”€â”€ ai/          # AI configs (lazy-loaded)
â”œâ”€â”€ data/        # Data files
â””â”€â”€ meta.toml    # Version tracking
```

Files are only created when you edit them - until then, they're loaded from templates. This makes updates easier, keeps your repository clean while maintaining full customizability. We also fixed `include` and added an optional `include` file you can add to the notebook, which will run all code inside it on startup.

## 4. Usability Improvements

Beyond the major features, numerous small improvements enhance the daily experience:

  * **Inline slider values**: `@manipulate` now shows current values next to sliders using flexbox
  * **File tab tooltips**: Hover over tabs to see full file paths
  * **Better mobile styling**: Improved responsive design for tablets and phones
  * **Consistent spacing**: All UI components now use the CSS variable system
  * **New cell menu**: It was quite annoying that the new cell menu changed the whole layout, so now it's fixed and opens a non obstructive hover menu
  * **More Tooltips**: Added quite a few Tooltips to explain the UI
  * **fixed include**: include wasn't working which should be fixed now

## Migration Guide

Upgrading is straightforward:

### For Books Without Custom Styling

1. Delete your `.name-bbook/` folder
2. A fresh structure will be created automatically the next time you run the book

### For Books With Custom Styles/Plugins

1. Backup your `book.jl` and custom styles
2. Delete the old folder structure
3. Recreate using the new plugin system if needed
4. Use `generate_style()` for styling customization

The new `meta.toml` file tracks versions and helps detect when migration is needed.

## Looking Ahead

This release shows how BonitoBook can be an extensible platform rather than a single-purpose tool. The next feature we plan to work on is a plugin for an LLM agent dashboard and polish the LLM integration. The plugin system opens up exciting possibilities for specialized notebook formats - we can't wait to see what the community builds!
¥14229×e’¥14229Ã¤root‚§payloadÈ#¬f“‹´16811889741371719933Çh¥14228³4063589640617343979Çh¥14229±59168681526903477Çh¥14225³7702272723770601600Çh¥14227²595013423889423680Çh¥14236´14468034022265603022Çh¥14232±95666805551695955Çh¥14235´15512323722984590581Çh¥14241³7747347880340899585Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¨markdown´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚd# Plugin System, Language Extensions & Simplified Styling

We're excited to announce a major update to BonitoBook that transforms it from a single-purpose notebook into an extensible platform. This release introduces a powerful plugin system, extensible language evaluators, and a completely redesigned styling architecture.

## 1. Plugin System: Transform Your Notebooks

The biggest new feature is a flexible plugin system that lets you completely customize how notebooks are rendered and behave. Plugins are modules that define custom book types and rendering logic.

### Creating a Plugin

Plugins live in `.name-bbook/book.jl` or a shared `.bbook/book.jl`:

```julia
module MyPlugin
using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end
# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module
```

From the [plugin guide](/howto/bonitobook-plugin):

### Built-in Plugin Examples

We've included three example plugins showcasing different use cases:

**Slideshow Plugin** - [Turn notebooks into presentations](../../examples/slideshow_example)

**Draggable Plugin** - [Interactive drag-and-drop layouts](../../examples/draggable_example)

**Book Plugin** - [Custom book formats with enhanced styling and table of content](../../examples/book-example)

## 2. Extensible Language Evaluators

BonitoBook now supports an extensible evaluator system for any language. We've moved language-specific code into extensions and made it easy to add new languages. Python support now needs using PythonCall.jl and CondaPkg:

```julia
# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution
```

Variables are automatically transferred between Python and Julia scopes, and the Python environment persists across cells.

### Creating Custom Language Evaluators

Adding a new language to BonitoBook is straightforward. From the [language evaluator guide](/howto/language-evaluator):

```julia
# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end
```

### Dependency Reduction

As part of this refactoring, we've moved WGLMakie to an extension, significantly reducing BonitoBook's dependency footprint and load times. Makie is still fully supported but only loaded when needed.

## 3. Simplified Styling System

The styling system has been completely redesigned around a single `generate_style()` function with comprehensive CSS variables.

### Before and After

**Before**: Styles scattered across `styles/style.jl` with hardcoded values everywhere

**After**: Single `style.jl` file with 70+ optional parameters:

```julia
style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object
```

### Lazy File Loading

The new folder structure uses lazy initialization:

```
.name-bbook/
â”œâ”€â”€ book.jl      # Plugin definition (optional)
â”œâ”€â”€ style.jl     # Styling (lazy-loaded from template)
â”œâ”€â”€ include.jl   # Optional include file, with code run on start
â”œâ”€â”€ ai/          # AI configs (lazy-loaded)
â”œâ”€â”€ data/        # Data files
â””â”€â”€ meta.toml    # Version tracking
```

Files are only created when you edit them - until then, they're loaded from templates. This makes updates easier, keeps your repository clean while maintaining full customizability. We also fixed `include` and added an optional `include` file you can add to the notebook, which will run all code inside it on startup.

## 4. Usability Improvements

Beyond the major features, numerous small improvements enhance the daily experience:

  * **Inline slider values**: `@manipulate` now shows current values next to sliders using flexbox
  * **File tab tooltips**: Hover over tabs to see full file paths
  * **Better mobile styling**: Improved responsive design for tablets and phones
  * **Consistent spacing**: All UI components now use the CSS variable system
  * **New cell menu**: It was quite annoying that the new cell menu changed the whole layout, so now it's fixed and opens a non obstructive hover menu
  * **More Tooltips**: Added quite a few Tooltips to explain the UI
  * **fixed include**: include wasn't working which should be fixed now

## Migration Guide

Upgrading is straightforward:

### For Books Without Custom Styling

1. Delete your `.name-bbook/` folder
2. A fresh structure will be created automatically the next time you run the book

### For Books With Custom Styles/Plugins

1. Backup your `book.jl` and custom styles
2. Delete the old folder structure
3. Recreate using the new plugin system if needed
4. Use `generate_style()` for styling customization

The new `meta.toml` file tracks versions and helps detect when migration is needed.

## Looking Ahead

This release shows how BonitoBook can be an extensible platform rather than a single-purpose tool. The next feature we plan to work on is a plugin for an LLM agent dashboard and polish the LLM integration. The plugin system opens up exciting possibilities for specialized notebook formats - we can't wait to see what the community builds!
¬formatOnTypeÃ»mouseWheelScrollSensitivity ³renderLineHighlight¤none­tabCompletion¢on³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂ´18004659695807228942Çh¥14226´11139844444574868115Çh¥14261Ú—import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = ((editor) => {
    const output_div = document.querySelector('[data-jscall-id="47"]');
    const logging_div = document.querySelector('[data-jscall-id="48"]');
    return import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
        const ee = new mod.EvalEditor(
            editor, output_div, logging_div, 'horizontal',
            __lookup_interpolated('7702272723770601600'), __lookup_interpolated('16811889741371719933'), __lookup_interpolated('14468034022265603022'),
            __lookup_interpolated('4063589640617343979'), __lookup_interpolated('595013423889423680'), true
        );
        const callback = ((editor) => {
    return import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(Monaco => {
        Monaco.register_cell_editor(editor, '57df4a9b-52ed-4f79-be23-89caf9037f0d')
        Monaco.setup_cell_editor(
            editor,
            '57df4a9b-52ed-4f79-be23-89caf9037f0d-hover', '57df4a9b-52ed-4f79-be23-89caf9037f0d-container', '57df4a9b-52ed-4f79-be23-89caf9037f0d-card-content',
            __lookup_interpolated('18004659695807228942'), __lookup_interpolated('11139844444574868115'),
            __lookup_interpolated('95666805551695955'),
            __lookup_interpolated('15512323722984590581')
        );
    })
}
);
        return callback(ee);
    })
})
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="46"]'), __lookup_interpolated('7747347880340899585'), init_func, 'horizontal', false, __lookup_interpolated('59168681526903477'));
    if (false) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2Èk’Ç@j“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14265Ç
e’¥14265Ôl ¤rootƒ£obsÇh¥14265§payloadÇ¬f“€Ù\return (val)=> {
    document.querySelector('[data-jscall-id="50"]').replaceChildren(val);
}ÙJ/home/runner/.julia/packages/Bonito/9fokY/src/rendering/observables.jl:117¨msg_type¡1ÈSk’ÇZj“Ù$0fd44c00-4a4a-47d0-9a88-a2a79384016c¥14267Ç$e’¥14267¼cell-logging hide-horizontal¤rootƒ£obsÇh¥14267§payloadÇÑf“€Ùvreturn value => {
    Bonito.update_node_attribute(document.querySelector('[data-jscall-id="49"]'), 'class', value);
}ÙU/home/runner/.julia/packages/Bonito/9fokY/src/rendering/hyperscript_integration.jl:92¨msg_type¡1ÈÀk’ÇDj“Ù$78972608-f4fd-4ee2-abf3-ac6ec1702c8c¥14251Çe’¥14251§default£sub‚§payloadÈ`f“‚´14900219157065722359Çh¥14251´16768606708239278237Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ`module MyPlugin
using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end
# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚLimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="64"]'), __lookup_interpolated('16768606708239278237'), init_func, 'horizontal', true, __lookup_interpolated('14900219157065722359'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2Èük’ÇDj“Ù$78972608-f4fd-4ee2-abf3-ac6ec1702c8c¥14252Çe’¥14252§default£sub‚§payloadÈœf“‚³5645295444475852986Çh¥14252´13025139105442451157Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÙŸ# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚKimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="81"]'), __lookup_interpolated('13025139105442451157'), init_func, 'horizontal', true, __lookup_interpolated('5645295444475852986'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2È‘k’ÇDj“Ù$78972608-f4fd-4ee2-abf3-ac6ec1702c8c¥14253Çe’¥14253§default£sub‚§payloadÈ1f“‚´17567018581489066466Çh¥14253´14974373255216571971Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ1# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚLimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="87"]'), __lookup_interpolated('14974373255216571971'), init_func, 'horizontal', true, __lookup_interpolated('17567018581489066466'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2È‚k’ÇDj“Ù$78972608-f4fd-4ee2-abf3-ac6ec1702c8c¥14254Çe’¥14254§default£sub‚§payloadÈ"f“‚³6095285558277869515Çh¥14254³6678249894446867528Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ%style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚKimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="101"]'), __lookup_interpolated('6678249894446867528'), init_func, 'horizontal', true, __lookup_interpolated('6095285558277869515'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2