’Ç,j“Ù$0e91fb1c-e818-4e61-8f52-09a50ac906e0€£subÇék’Ç-j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd€¤root‚§payloadÇ¡f“€Ù]    window.BONITO_EXPORT_MODE = true;
    document.body.classList.add('bonito-export-mode');
Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:96¨msg_type¡2È¨k’Ç-j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd€¤root‚§payloadÈ_f“€Ú(() => {
    document.querySelector('[data-jscall-id="29"]')['onclick'] = () => {
    const a = document.createElement('a');
a.href = '../../bonito/md/post3331658757419362023.md';
a.download = 'post.md';
document.body.appendChild(a);
a.click();
document.body.removeChild(a);

}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:78¨msg_type¡2Çïk’Ç-j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd€¤root‚§payloadÇ§f“€Ùc(() => {
    document.querySelector('[data-jscall-id="31"]')['onclick'] = ()=> window.print();
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/export.jl:85¨msg_type¡2È‚k’Ç=j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28744×e’¥28744Â¤root‚§payloadÈ)f“³4707173526810928062Çh¥28744ÚÇ(() => {
    document.querySelector('[data-jscall-id="37"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('4707173526810928062').value;
    __lookup_interpolated('4707173526810928062').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2È…k’Ç=j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28745×e’¥28745Â¤root‚§payloadÈ,f“´15856766424159626571Çh¥28745ÚÉ(() => {
    document.querySelector('[data-jscall-id="39"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('15856766424159626571').value;
    __lookup_interpolated('15856766424159626571').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2È…k’Ç=j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28743×e’¥28743Ã¤root‚§payloadÈ,f“´12027507928027925540Çh¥28743ÚÉ(() => {
    document.querySelector('[data-jscall-id="41"]')['onclick'] = event=> {
    const button = event.target.closest('button');
    const newValue = !__lookup_interpolated('12027507928027925540').value;
    __lookup_interpolated('12027507928027925540').notify(newValue);

    // Toggle the active class based on new value
    if (newValue) {
        button.classList.add('active');
    } else {
        button.classList.remove('active');
    }
}
})()Ù>/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:21¨msg_type¡2ÈJk’Ç=j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28746×e’¥28746Â¤root‚§payloadÇòf“³5234485240208619622Çh¥28746Ù(() => {
    document.querySelector('[data-jscall-id="43"]')['onclick'] = event=> __lookup_interpolated('5234485240208619622').notify(true)
})()Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:451¨msg_type¡2È=‚k’Èwj“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cdŠ¥28711Çe’¥28711§default¥28727×e’¥28727Â¥28747×e’¥28747Â¥28722×e’¥28722Â¥28715×e’¥28715Ã¥28712×e’¥28712Â¥28718È¨e’¥28718Ú# Plugin System, Language Extensions & Simplified Styling

We're excited to announce a major update to [BonitoBook](https://bonitobook.org) that transforms it from a single-purpose notebook into an extensible platform. This release introduces a powerful plugin system, extensible language evaluators, and a completely redesigned styling architecture.

## 1. Plugin System: Transform Your Notebooks

The biggest new feature is a flexible plugin system that lets you completely customize how notebooks are rendered and behave. Plugins are modules that define custom book types and rendering logic.

### Creating a Plugin

Plugins live in `.name-bbook/book.jl` or a shared `.bbook/book.jl`:

```julia
module MyPlugin

using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end

# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module
```

From the [plugin guide](../../bonitobook-plugin):

### Built-in Plugin Examples

We've included three example plugins showcasing different use cases:

**Slideshow Plugin** - [Turn notebooks into presentations](../../slideshow_example) **Draggable Plugin** - [Interactive drag-and-drop layouts](../../draggable_example) **Book Plugin** - [Custom book formats with enhanced styling and table of content](../../book-example)

## 2. Extensible Language Evaluators

BonitoBook now supports multiple programming languages through an extensible evaluator system. We've moved language-specific code into extensions and made it easy to add new languages.

### Python Support

Python is now supported via an extension using PythonCall.jl:

```julia
# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution
```

Variables are automatically transferred between Python and Julia scopes, and the Python environment persists across cells.

### Creating Custom Language Evaluators

Adding a new language to BonitoBook is straightforward. From the [language evaluator guide](../../language-evaluator):

```julia
# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end
```

Register it in `ALL_LANGUAGES` and it appears in the language dropdown automatically.

### Dependency Reduction

As part of this refactoring, we've moved WGLMakie to an extension, significantly reducing BonitoBook's dependency footprint and load times. Makie is still fully supported but only loaded when needed.

## 3. Simplified Styling System

The styling system has been completely redesigned around a single `generate_style()` function with comprehensive CSS variables.

### Before and After

**Before**: Styles scattered across `styles/style.jl` with hardcoded values everywhere

**After**: Single `style.jl` file with 70+ optional parameters:

```julia
style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object
```

### Lazy File Loading

The new folder structure uses lazy initialization:

```
.name-bbook/
â”œâ”€â”€ book.jl      # Plugin definition (optional)
â”œâ”€â”€ style.jl     # Styling (lazy-loaded from template)
â”œâ”€â”€ include.jl   # Optional include file, with code run on start
â”œâ”€â”€ ai/          # AI configs (lazy-loaded)
â”œâ”€â”€ data/        # Data files
â””â”€â”€ meta.toml    # Version tracking
```

Files are only created when you edit them - until then, they're loaded from templates. This makes updates easier, keeps your repository clean while maintaining full customizability. We also fixed `include` and added an optional `include` file you can add to the notebook, which will run all code inside it on startup.

## 4. Usability Improvements

Beyond the major features, numerous small improvements enhance the daily experience:

  * **Inline slider values**: `@manipulate` now shows current values next to sliders using flexbox
  * **File tab tooltips**: Hover over tabs to see full file paths
  * **Better mobile styling**: Improved responsive design for tablets and phones
  * **Consistent spacing**: All UI components now use the CSS variable system
  * **New cell menu**: It was quite annoying that the new cell menu changed the whole layout, so now it's fixed and opens a non obstructive hover menu
  * **More Tooltips**: Added quite a few Tooltips to explain the UI

## Migration Guide

Upgrading is straightforward:

### For Books Without Custom Styling

1. Delete your `.name-bbook/` folder
2. A fresh structure will be created automatically the next time you run the book

### For Books With Custom Styles/Plugins

1. Backup your `book.jl` and custom styles
2. Delete the old folder structure
3. Recreate using the new plugin system if needed
4. Use `generate_style()` for styling customization

The new `meta.toml` file tracks versions and helps detect when migration is needed.

## Looking Ahead

This release shows how BonitoBook can be an extensible platform rather than a single-purpose tool. The next feature we plan to work on is a plugin for an LLM agent dashboard and polish the LLM integration. The plugin system opens up exciting possibilities for specialized notebook formats - we can't wait to see what the community builds!
¥28721×e’¥28721Ã¥28714×e’¥28714€¥28713×e’¥28713€¤root‚§payloadÈ#îf“‹³9021941684995018852Çh¥28722³9618024011851053886Çh¥28714´14833098115278232835Çh¥28718´18027256795058736959Çh¥28715³2761350870039301476Çh¥28712³2711156294542225263Çh¥28747´12695804964468226928Çh¥28711´16337403716060960894Çh¥28721³7998634671368760116Çh¥28727³8606402846949270076Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¨markdown´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ# Plugin System, Language Extensions & Simplified Styling

We're excited to announce a major update to [BonitoBook](https://bonitobook.org) that transforms it from a single-purpose notebook into an extensible platform. This release introduces a powerful plugin system, extensible language evaluators, and a completely redesigned styling architecture.

## 1. Plugin System: Transform Your Notebooks

The biggest new feature is a flexible plugin system that lets you completely customize how notebooks are rendered and behave. Plugins are modules that define custom book types and rendering logic.

### Creating a Plugin

Plugins live in `.name-bbook/book.jl` or a shared `.bbook/book.jl`:

```julia
module MyPlugin

using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end

# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module
```

From the [plugin guide](../../bonitobook-plugin):

### Built-in Plugin Examples

We've included three example plugins showcasing different use cases:

**Slideshow Plugin** - [Turn notebooks into presentations](../../slideshow_example) **Draggable Plugin** - [Interactive drag-and-drop layouts](../../draggable_example) **Book Plugin** - [Custom book formats with enhanced styling and table of content](../../book-example)

## 2. Extensible Language Evaluators

BonitoBook now supports multiple programming languages through an extensible evaluator system. We've moved language-specific code into extensions and made it easy to add new languages.

### Python Support

Python is now supported via an extension using PythonCall.jl:

```julia
# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution
```

Variables are automatically transferred between Python and Julia scopes, and the Python environment persists across cells.

### Creating Custom Language Evaluators

Adding a new language to BonitoBook is straightforward. From the [language evaluator guide](../../language-evaluator):

```julia
# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end
```

Register it in `ALL_LANGUAGES` and it appears in the language dropdown automatically.

### Dependency Reduction

As part of this refactoring, we've moved WGLMakie to an extension, significantly reducing BonitoBook's dependency footprint and load times. Makie is still fully supported but only loaded when needed.

## 3. Simplified Styling System

The styling system has been completely redesigned around a single `generate_style()` function with comprehensive CSS variables.

### Before and After

**Before**: Styles scattered across `styles/style.jl` with hardcoded values everywhere

**After**: Single `style.jl` file with 70+ optional parameters:

```julia
style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object
```

### Lazy File Loading

The new folder structure uses lazy initialization:

```
.name-bbook/
â”œâ”€â”€ book.jl      # Plugin definition (optional)
â”œâ”€â”€ style.jl     # Styling (lazy-loaded from template)
â”œâ”€â”€ include.jl   # Optional include file, with code run on start
â”œâ”€â”€ ai/          # AI configs (lazy-loaded)
â”œâ”€â”€ data/        # Data files
â””â”€â”€ meta.toml    # Version tracking
```

Files are only created when you edit them - until then, they're loaded from templates. This makes updates easier, keeps your repository clean while maintaining full customizability. We also fixed `include` and added an optional `include` file you can add to the notebook, which will run all code inside it on startup.

## 4. Usability Improvements

Beyond the major features, numerous small improvements enhance the daily experience:

  * **Inline slider values**: `@manipulate` now shows current values next to sliders using flexbox
  * **File tab tooltips**: Hover over tabs to see full file paths
  * **Better mobile styling**: Improved responsive design for tablets and phones
  * **Consistent spacing**: All UI components now use the CSS variable system
  * **New cell menu**: It was quite annoying that the new cell menu changed the whole layout, so now it's fixed and opens a non obstructive hover menu
  * **More Tooltips**: Added quite a few Tooltips to explain the UI

## Migration Guide

Upgrading is straightforward:

### For Books Without Custom Styling

1. Delete your `.name-bbook/` folder
2. A fresh structure will be created automatically the next time you run the book

### For Books With Custom Styles/Plugins

1. Backup your `book.jl` and custom styles
2. Delete the old folder structure
3. Recreate using the new plugin system if needed
4. Use `generate_style()` for styling customization

The new `meta.toml` file tracks versions and helps detect when migration is needed.

## Looking Ahead

This release shows how BonitoBook can be an extensible platform rather than a single-purpose tool. The next feature we plan to work on is a plugin for an LLM agent dashboard and polish the LLM integration. The plugin system opens up exciting possibilities for specialized notebook formats - we can't wait to see what the community builds!
¬formatOnTypeÃ»mouseWheelScrollSensitivity ³renderLineHighlight¤none­tabCompletion¢on³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂ³7643179677838718022Çh¥28713Ú›import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = ((editor) => {
    const output_div = document.querySelector('[data-jscall-id="48"]');
    const logging_div = document.querySelector('[data-jscall-id="49"]');
    return import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
        const ee = new mod.EvalEditor(
            editor, output_div, logging_div, 'horizontal',
            __lookup_interpolated('7643179677838718022'), __lookup_interpolated('9618024011851053886'), __lookup_interpolated('14833098115278232835'),
            __lookup_interpolated('18027256795058736959'), __lookup_interpolated('9021941684995018852'), true
        );
        const callback = ((editor) => {
    return import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(Monaco => {
        Monaco.register_cell_editor(editor, 'f3535e44-9af6-4ab9-97e6-2a96743620a5')
        Monaco.setup_cell_editor(
            editor,
            'f3535e44-9af6-4ab9-97e6-2a96743620a5-hover', 'f3535e44-9af6-4ab9-97e6-2a96743620a5-container', 'f3535e44-9af6-4ab9-97e6-2a96743620a5-card-content',
            __lookup_interpolated('2761350870039301476'), __lookup_interpolated('2711156294542225263'),
            __lookup_interpolated('16337403716060960894'),
            __lookup_interpolated('7998634671368760116')
        );
    })
}
);
        return callback(ee);
    })
})
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="47"]'), __lookup_interpolated('8606402846949270076'), init_func, 'horizontal', false, __lookup_interpolated('12695804964468226928'));
    if (false) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2Èk’Ç@j“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28751Ç
e’¥28751Ôl ¤rootƒ£obsÇh¥28751§payloadÇ¬f“€Ù\return (val)=> {
    document.querySelector('[data-jscall-id="51"]').replaceChildren(val);
}ÙJ/home/runner/.julia/packages/Bonito/Wixad/src/rendering/observables.jl:117¨msg_type¡1ÈFk’ÇMj“Ù$1b474b5c-7109-4a21-9778-02d4b29e33cd¥28753Çe’¥28753¯hide-horizontal¤rootƒ£obsÇh¥28753§payloadÇÑf“€Ùvreturn value => {
    Bonito.update_node_attribute(document.querySelector('[data-jscall-id="50"]'), 'class', value);
}ÙU/home/runner/.julia/packages/Bonito/Wixad/src/rendering/hyperscript_integration.jl:92¨msg_type¡1ÈÀk’ÇDj“Ù$0e91fb1c-e818-4e61-8f52-09a50ac906e0¥28737Çe’¥28737§default£sub‚§payloadÈ`f“‚³1298533970613584400Çh¥28737´11705794184592072000Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚbmodule MyPlugin

using Bonito, BonitoBook

struct MyBook <: BonitoBook.AbstractBook
    book::BonitoBook.Book
end

# Plugin initialization with custom kwargs
create_book(book::BonitoBook.Book; kwargs...) = MyBook(book)

function Bonito.jsrender(session::Session, my_book::MyBook)
    # Custom rendering logic
    container = DOM.div(my_book.book.cells..., class="my-custom-layout")
    # Re-use any standard setup for e.g. autocomplete from BonitoBook (optional)
    elements = BonitoBook.standard_setup!(session, my_book.book)
    return Bonito.jsrender(session, DOM.div(elements, container))
end
end # module¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚKimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="66"]'), __lookup_interpolated('11705794184592072000'), init_func, 'horizontal', true, __lookup_interpolated('1298533970613584400'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2Èşk’ÇDj“Ù$0e91fb1c-e818-4e61-8f52-09a50ac906e0¥28738Çe’¥28738§default£sub‚§payloadÈf“‚´13783288541761066548Çh¥28738´13025139105442451157Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÙŸ# no-eval
using PythonCall, CondaPkg, BonitoBook # needs to be loaded first now
BonitoBook.book("book-with-python.md") # book now with enabled Python execution¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚLimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="83"]'), __lookup_interpolated('13025139105442451157'), init_func, 'horizontal', true, __lookup_interpolated('13783288541761066548'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2Èk’ÇDj“Ù$0e91fb1c-e818-4e61-8f52-09a50ac906e0¥28739Çe’¥28739§default£sub‚§payloadÈ/f“‚´14974373255216571971Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ1# ext/BonitoBookShellExt.jl
module BonitoBookShellExt
using BonitoBook
struct ShellEval <: BonitoBook.LanguageEval end
function BonitoBook.eval_code(::ShellEval, mod::Module, file::String, line::Int, source::String)
    return read(`bash -c $source`, String)
end
get_language_evaluator() = ShellEval()
end¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂ³5011005137323468929Çh¥28739ÚKimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="89"]'), __lookup_interpolated('14974373255216571971'), init_func, 'horizontal', true, __lookup_interpolated('5011005137323468929'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2È„k’ÇDj“Ù$0e91fb1c-e818-4e61-8f52-09a50ac906e0¥28740Çe’¥28740§default£sub‚§payloadÈ$f“‚´16040931762825273832Çh¥28740³6678249894446867528Ş ³overviewRulerBorderÂ¬autoSurround¯languageDefined¨language¥julia´lineDecorationsWidth µfastScrollSensitivity ªautoIndent¤full®wordWrapColumnP²overviewRulerLanes ¨readOnlyÃ¨wordWrap®wordWrapColumn±autoClosingQuotes¦always©scrollbarƒªhorizontal¦hidden¨vertical¦hidden±verticalHasArrowsÂ´scrollBeyondLastLineÂ¥valueÚ%style = BonitoBook.generate_style(current_book(),
    light_theme = nothing,  # Auto-detect system preference

    # Override specific colors
    bg_primary_light = "#ffffff",
    accent_blue_light = "#0366d6",

    # Customize spacing
    spacing_lg = "1rem",
    spacing_xs = "0.25rem",
    # Shadow patterns
    shadow_color_soft_light = "rgba(0, 0, 51, 0.2)",
    shadow_blur_sm = "3px",
)
# still possible to completely overwrite anything not covered om the above function
Styles(style, YourCustomStyle) # file needs to return any Styles object¬formatOnTypeÃ»mouseWheelScrollSensitivity «lineNumbers£off³renderLineHighlight¤none³autoClosingBrackets¦always³disableLayerHintingÃ¹hideCursorInOverviewRulerÃ¯automaticLayoutÃ¶scrollBeyondLastColumn ·bracketPairColorization§enabledÃ§minimap§enabledÂÚLimport(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href).then(mod => {
    const init_func = (editor) => {
    Promise.all([import(new URL('../../bonito/js/Monaco.bundled5076920984461186154.js', window.location.href).href), editor.monaco, editor.editor]).then(([mod, monaco, e]) => {
        // Resize editor to fit content
        mod.resize_to_lines(e, monaco, editor.editor_div);
    });
}
;
    const editor = new mod.MonacoEditor(document.querySelector('[data-jscall-id="105"]'), __lookup_interpolated('6678249894446867528'), init_func, 'horizontal', true, __lookup_interpolated('16040931762825273832'));
    if (true) {
        // return promise tho wait for the editor to be ready
        return editor.editor;
    }
    // if invisible, editor doesn't get initialized, so we dont wait on it
})
Ù?/home/runner/work/BonitoBook.jl/BonitoBook.jl/src/editor.jl:113¨msg_type¡2